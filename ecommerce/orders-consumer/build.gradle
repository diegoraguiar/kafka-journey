plugins {
    id 'com.google.cloud.tools.jib' version '2.6.0'
}

jib {
    to {
        image = 'local/orders-consumer:local'
    }
    container {
        mainClass = 'org.examples.kafka.OrderConsumer'
        creationTime = 'USE_CURRENT_TIMESTAMP'
    }
}
